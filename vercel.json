{
  "version": 2,
  "builds": [
    {
      "src": "tbi_frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "tbi_frontend/build/$1" }
  ]
}
